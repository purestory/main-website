# 🚨 중요: 이 파일을 삭제하지 마세요! (DO NOT DELETE THIS FILE!)
# 이 파일은 자동 배포를 담당합니다. 삭제 시 수동으로 서버 업데이트 필요.
# This file handles automatic deployment. Manual server updates required if deleted.

name: Auto Deploy to Server

on:
  push:
    branches: [ jules_wip_4591432813991362156 ]
  pull_request:
    branches: [ jules_wip_4591432813991362156 ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.PORT }}
        script: |
          cd /home/purestory/main-website
          git fetch origin
          git pull origin jules_wip_4591432813991362156
          echo "✅ Auto-deployment completed at $(date)" 